# CURRENT：期刊来源、加工优先级与服务额度

## Goal / version tuple

- 用户要求：在既有 journal-onboarding 分支优先跑通来源版权矩阵、AI 加工优先级、服务额度页；Topic Hub 和机器访问分析第二批。
- Branch：`codex/journal-onboarding`；增量基线：`261a38fa9fa5e97a099c59a082447ed87b43de05`。
- 验收代码：`6fd90237ebda4094eefb6b947a797e4fdcecc6ae`；交付另含本验收文档提交，以分支 Git 元数据为准。
- 交付入口：[既有 PR #1](https://github.com/Nanqing96/openscience/pull/1)；base 为 `release/academic-identity-ror-20260902`。不自动合并或部署。
- Production release / rollback：本轮未读取、未修改、未验收；不能将本地结果表述为生产上线。

## Done

- 先阅读既有设计、handoff、runbook、需求基线相关章节及 journal 代码，沿原有模块实现增量。
- 多来源矩阵：类型、可信度、逐项权限、许可、证据、到期、主要来源与授权历史；服务端保守计算加工和公开范围。
- 主来源绑定实际文本摘要及文件/链接；新增辅助材料保留旧主来源，不能用另一来源的授权放行当前文本。
- 来源改动清除旧权限和审核，重新核验后恢复加工；新文件先私有暂存，确认该文件授权后免费解析。保留幂等回放、失败/取消/超时后的重新上传。
- 授权到期、撤回或范围缩小时关闭受影响解读；通用研究详情、素材、Evidence、Explore、Fork、Export、公开编辑集合同步检查当前授权。书目目录保留原论文身份。
- 加工页：八维解释、分数/等级、阻断与缺失信息、重点标记、延后/恢复、分页和显式确认入队；不自动触发 AI。
- 服务页：真实有效余额、预占、消耗、到期、月度使用、批次、账本和存储；Free/Starter/Pro/Premium/Custom 为人工申请方案。
- 沿用整数账本：成功草稿消耗 1 篇，失败/取消释放预占；服务申请不自动开通或扣款。
- 复用现有 JSON、事件及账本；没有新迁移、依赖或生产配置变更。独立期刊 CI 增加真实浏览器流程。

## Evidence — 2026-09-22 local

- 依赖使用锁定的 pnpm 9.15；隔离 PostgreSQL 16 的 37 个核心迁移全部应用，Prisma core/search 客户端生成通过。
- Domain 完整回归：574/574；API 完整回归：117/117（默认跳过的两条浏览器测试另行真实运行）。
- Web 完整回归：471/471，加 5/5 Node 资源检查；期刊 Worker：4/4。
- 真实 Chromium：2/2 场景，9 个页面 × 桌面 1440 / 手机 375，均无横向溢出；21 张合成数据截图保存在忽略目录 `apps/web/test/visual/out/journals/`。
- 浏览器覆盖真实会话/CSRF、来源登记、来源文本修改后禁用生成及重新绑定、重点/延期持久化、入队预占与取消释放、服务申请，以及既有纠错、审核、限制公开与 sitemap。
- 后端与 Web 正式构建、Domain/API/Web 类型检查、改动文件 ESLint、Git diff 检查通过；文档 lint 247 份、docs-sync 8 项及索引检查通过。
- 本地 API 套件按文件顺序运行，保留测试内部并发场景；Windows 隔离数据库连接池设为 4，避免本地高并发建连抖动。CI 使用独立 PostgreSQL。
- 回归中修复了权限过期的通用入口绕过、首次辅助材料影响主来源、上传旧授权继承与幂等回放、页面控件无障碍名称等问题；没有放宽授权或科学证据校验来通过测试。

## Constraints / open risks

- 所有数据为本地合成来源；真实模型、生产扫描/PDF/OCR sidecar、真实来源和公网运行尚未验收。
- 登记链接不会自动下载材料；生成仍依据已绑定主来源，不能把登记材料或评分视为已完成解析、科学质量或 AI 引用证据。
- Topic Hub、机器访问分析、多刊共享额度、在线支付、自动续费、小数/多计量计费仍未实现；展示价值等缺乏可靠数据的评分维度明确为未知。
- 采用矩阵后不可直接回退至忽略矩阵和动态到期检查的旧应用；先保留权限检查或限制相关公开内容。功能开关不能替代公开读检查。
- 未读取 `.env` 或生产密钥，未改变其他工作区或生产服务器。Git 提交使用临时 Codex 作者标识，不修改全局 Git 配置。

## Next action / read-first

1. 在 PR #1 审阅增量，按 [运行手册](../runbooks/journal-onboarding.md)完成真实模型、解析器与来源试点验收后再安排部署。
2. 选择 3–5 家期刊核对授权证据、加工成本和人工审核流程，确定服务报价及额度周期规则。
3. 第二批再实现 Topic Hub 和机器访问分析，保持机器请求次数与真实引用的口径区分。
